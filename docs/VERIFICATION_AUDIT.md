# ZENITH requirement and correctness audit

This matrix states exactly what the prototype claims and how that claim is checked.

| Requirement or question | Implemented behavior | Verification evidence |
|---|---|---|
| Camera-only target input | In `YOLO CUSTOM`, guidance receives a trained model's box, confidence, bearing, the later signal-resolved model, pose-free pinhole estimate, filtered track, uncertainty, and our integrated state. The model input is only the clean BGR output of the rigid 90° camera. The deterministic fallback is separately labeled `SYNTHETIC BOX + POSE`. | The YOLO adapter mapping and runtime-isolation tests prove the model box enters the sensor path without simulator pose and that runtime sensing does not call the synthetic detector; the fixed-boresight test proves a target behind the airframe cannot remain locked. |
| Honest detector boundary | The bundled custom Ultralytics weights detect one generic `aerial_target` class. Runtime `render_bgr()` exposes pixels without annotations; exact labels exist only in the offline dataset generator. The small-target crop is fixed at sensor boresight rather than target truth. Missing weights or ML dependencies reject YOLO selection instead of silently relabeling the fallback. | `test_bundled_yolo_weights_detect_a_rendered_target`, `test_runtime_sensor_renderer_returns_pixels_without_annotations`, `test_yolo_small_target_crop_is_fixed_at_sensor_boresight`, `test_yolo_runtime_does_not_call_synthetic_box_detector`, `test_yolo_box_maps_to_sensor_without_simulator_pose`, and `test_unloaded_ai_backend_cannot_be_falsely_selected` |
| Multiple enemy contacts | Setup selects 1–3 enemies. Each receives an independent detection, signal timer, range/velocity track, uncertainty, oval solution, and guidance candidate. At most one pair is compared at equal horizons in the common frozen image plane. Mutual-center containment plus a reachable shared centroid activates cyan overlap pursuit; entering either member's smaller nested oval creates a sticky committed lock. | Same-horizon/mutual-center geometry test plus three-contact independent-track, shared-aim containment, and commit-state regression tests. |
| Detect, then query signals | Identity and metric guidance become available only after visual acquisition and the configured lookup delay. | `test_signal_lookup_precedes_identity` |
| Estimated enemy coordinates | The Target panel shows camera-derived world XYZ. Truth is available only inside the expanded verification window. | Minimum-window widget test and saved-frame verification tests. |
| Frozen-now oval definition | At 60 Hz, the current sensor pose is treated as fixed and the target's future pixel region is projected into that frame. | Perspective projection and common-plane guidance tests. |
| Real ellipses on one target plane | The rendered border uses `center + a cos(t) X + b sin(t) Y` and is back-projected onto the plane through the estimated target, perpendicular to the sensor. | Ellipse-equation and target-plane tests. |
| Enemy cannot escape under declared limits | Propulsion support, closest permitted perspective depth, and bounded position/velocity uncertainty form a conservative outer border. Camera-plane crossings are reported as unbounded. | Allowed-acceleration containment, camera-crossing, and six two-second drone-scenario checks. |
| Why four dots are not enough | Four large cardinal points remain visible, but diagonal points lie outside their connecting diamond. All 96 rendered directions decide the border. | `test_entire_oval_color_reports_complete_edge_reachability` deliberately fails a diagonal while keeping `4/4`. |
| Reachability color | Every passing edge segment is green and every failing segment is red. Only a fully green `96/96` loop is selectable and receives a green tint; red never fills an interior. A partial loop is mixed, and grey means no finite valid bound. | Complete-edge color/fill, 96-segment rendering, and unbounded-prediction tests. |
| Weighted largest-oval guidance | The largest green horizon is selected. Its aim is confidence-weighted toward measured likely motion and limited to 65% of the ellipse. | Weighted-oval selection and normalized-radius assertions. |
| Terminal guidance | The normal rule changes to camera-derived collision lead when our one-second reach enters the smallest region; `0.75 s` TTC is the safety fallback. Solid rockets use proportional navigation throughout. | Default and catalogue interception tests. |
| Gravity and aerodynamics | Gravity is continuously explicit. Rotorcraft spend thrust to hover; wings create speed/stall-dependent lift; rockets have no wing lift. | Gravity, hover, glide, and stall tests. |
| Level vectored-drone start and stable camera | Aegis-Q4 and Smart Evader begin at half maximum speed with zero vertical velocity. Forward thrust produces a nose-down attitude, downward requests cannot reflect upward, and a fixed 6° mount plus 24° body envelope retains one continuous non-gimballed lock in the baseline case. | Exact startup, pitch-direction, downward-thrust, mount-angle, and zero-reacquisition regression tests. |
| Literal one-shot rockets | Both rocket models can be our interceptor or the target. They ignite automatically, burn for a finite time, cannot throttle/cut/restart/reverse, and use separately limited RCS. | Booster burnout, RCS supply, reverse rejection, both-side catalogue, and rocket interception tests. |
| Honest target loss | Range, guidance, and live ovals become invalid. Search uses only previous image bearing/rate, holds altitude and about half maximum speed, and only a new detection reacquires. | Occlusion, reacquisition, horizon, half-speed cruise, and manual-search tests. |
| Onboard boresight versus guidance aim | The fixed cross is labeled `SENSOR BORESIGHT` and exists only onboard. A separate diamond marks the world-space guidance aim. | Onboard/spectator reticle rendering test. |
| Expandable UI and minimap | All four core panels, including Calculations, start open; each remains individually collapsible. Minimap, settings, and verification start collapsed. The map uses every contact's estimated X/Z, the sensor FOV, and altitude-coded markers. | Collapsible widget and three-contact rendering at 1050×700. |
| Presentation zoom | The wheel changes only the renderer's bounded 24°–100° field of view. Sensor HFOV and nose boresight remain unchanged; `C` resets zoom. | Presentation-only zoom, bounds, projection, and sensor-invariance test. |
| Intelligent adversary mode | `TRICKY AI` assumes the target knows true relative range/closing speed and deterministically selects jinks, climbs, sprints, and brake traps. Commands still pass through the selected target's acceleration, speed, turn, drag, gravity, and airbrake model. Some matchups can escape; no truth is fed to defense guidance. | Determinism, decision diversity, speed-limit, airbrake, successful and escaped stress-case tests. |
| Two-second presentation proof | `G` records the old virtual camera and +2 s oval. The inset keeps that camera fixed while the live vehicle/camera moves, then checks truth only at saved `T+2`. | Recorded-camera immutability and all-drone-scenario containment tests. |
| Stable oval display | Floating-point detector spans and lower-gain drone velocity tracking prevent whole-pixel range steps from creating one-frame oval inflation. Fast incoming rockets retain a separate responsive track mode. | Subpixel range-continuity and frame-stable rendered-radius tests. |
| End-to-end interception | The deterministic fallback keeps all seven established verifier scenarios and catalogue tests unchanged. The trained YOLO backend's final checked-in report also signal-identifies and physically contacts the target in all seven behaviors. Measured box/range errors remain in the report rather than being replaced by truth-derived boxes. | `artifacts/verification.csv`, `artifacts/yolo_verification.csv`, `verify_prototype.py --duration 30`, `verify_prototype.py --duration 30 --detector yolo`, and the scenario/catalogue suite. |

## Claim boundary

“The enemy cannot escape” means it cannot escape the displayed outer region while obeying the simulator's published propulsion model and configured sensor-error bound. It is not a claim about unmodeled wind, damage, deceptive emissions, actuator faults, or real hardware.

The four cardinal dots are a presentation aid. The complete 96-direction border decides selection. A mixed or fully red oval is never selected even if its cardinal summary says `4/4`; guidance then reports `NO GUARANTEED OVAL` and uses the unchanged-motion fallback until a complete green solution or terminal condition exists.

The verification inset and truth coordinates are simulation-only audit tools. Neither can feed the detector, range estimate, target track, oval construction, or defense maneuver command. Runtime YOLO similarly cannot read the offline annotation output. Its one-time actor association seed exists only to connect a generic image track to the simulated signal responder; seed coordinates cannot enter range, filtering, ovals, priority, or maneuver commands. The explicitly labeled `TRICKY AI` scenario separately permits the adversary's own controller to observe the interceptor because that is the test assumption.
